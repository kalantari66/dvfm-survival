# Data

`examples/` contains small generated files for smoke tests.

Put licensed real datasets in `raw/`. Files under `raw/` are ignored by Git so private or restricted datasets are not accidentally committed.
